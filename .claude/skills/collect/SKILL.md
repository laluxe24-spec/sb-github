---
name: collect
description: 元データを集める手順書。SNSの投稿・スクショを集める、TikTok・YouTubeの動画やKindleの本を文字起こしして、01_Sourcesに保存する。「〇〇の投稿集めて」「この投稿拾える?」「〇〇の投稿見たい」「新着取って」「@〇〇を追加して」「この動画/本を文字起こしして」「この本読み込んで」の時に使う。収集・文字起こしのプログラムを動かす時は、必ず先にこれを読む。
---

# 元データを集める

## どのファイルを読むか

| どこから | 読むファイル |
|---|---|
| X(投稿・スクショ) | `X.md` |
| TikTokの動画 | `TikTok.md` |
| YouTubeの動画 | `YouTube.md` |
| Kindleの本 | `Kindle.md` |

新しい媒体(Threads・Instagramなど)から集める時は、同じ形で`<媒体名>.md`をこのフォルダに作ってから始める(媒体ごとに1ファイル)。

## 共通ルール

- 集めるだけ。分析やナレッジはここでやらない(副業の分析・ネタはresearch、りゅーじ君の成長のための知識はmy-knowledge)
- **集める時は使い道を決めない。** 副業に使うか、りゅーじ君の成長に使うかは、使う側が1件ずつ決める
- 誰をいつも追っているかは`01_Sources/収集リスト.md`。人を増やす・やめる時は、そこも書き換える
- 保存先は`01_Sources/<媒体>/data/`。リサーチのために仮で集める人は`01_Sources/<媒体>/候補/<プロジェクト>/`(毎朝の自動収集には入らない)
- 要約や解釈は加えず、そのまま残す
- 確認を取らずに最後まで進めてよい(消す時だけは確認する)

## 動画の文字起こし(TikTok・YouTube共通)

音声だけダウンロードして、文字にする。その媒体のフォルダ(`01_Sources/03_TikTok`など)で実行する。

- 1本の時: `python3 scripts/transcribe.py "<動画URL>"`
- まとめて処理する時: URLを1行1つ書いたテキストファイルを作って `python3 scripts/transcribe.py --file urls.txt`
- できるもの:
  - `data/@<アカウント名>/transcripts/<動画ID>.md`: 文字起こし(URL・投稿者・タイトル・投稿日つき)
  - `data/@<アカウント名>/manifest.json`: そのアカウントの動画一覧
  - 音声ファイルは自動で消える
- 処理済みの動画は「スキップ(処理済み)」と出て正常終了する。エラーではない

使っている道具:
- `yt-dlp`(brew): 音声だけダウンロード
- `ffmpeg`(brew): 音声の変換
- `mlx-whisper`(pip, Apple Silicon専用): 文字起こし。モデルは`mlx-community/whisper-large-v3-turbo`

注意点:
- 初回はWhisperモデル(数百MB〜1GB)のダウンロードで時間がかかる
- 大量処理はメモリ対策済み(空きメモリ800MB未満で待機、10本ごとに再起動)。重いアプリ(Chromeのタブ、Claudeデスクトップアプリ)を閉じておくと落ちにくい
