# 操作メモ(Kindle本のスクショ取り込み・重複整理の再現手順)

このメモは、Claude自身が次回以降のセッションでも同じ作業を再現できるようにするための手順書。
複数の本を扱う前提のフォルダ構成になっている。

## フォルダ構成
```
02_Sources/01_Kindle/
├── docs/                       共通ドキュメント(このファイル)
├── scripts/
│   ├── kindle_screenshot.py       共通のキャプチャスクリプト(全ての本で使い回す)
│   ├── ocr_book.py                縦書き本用OCR(tesseract jpn_vert)
│   ├── ocr_book_horizontal.py     横書き本用OCR(macOS Vision)
│   └── venv/                     スクリプト実行用のPython仮想環境(Pillow, pyobjc-Vision等)
└── data/
    └── <本のタイトル>/
        ├── screenshots/          その本のページ画像(0001.png, 0002.png, ...)
        │                          ※OCRが終わったら削除してよい(手順6参照)
        └── 本文.md               OCRで書き起こした全ページのテキスト(誤字混じり)
                                   ※スクショを消した後も本の内容が追える「データ」本体
```
新しい本を取り込む場合は `data/<本のタイトル>/` が自動的に作られる(手順1参照)。

考え方まとめ(本の要約)は、このプロジェクト内には置かず、`03_Knowledge/`配下のジャンル別フォルダ(例: `03_Knowledge/02_Psychology/`, `03_Knowledge/03_Romance/`)に直接作成する運用にしている(ファイル名は`<本のタイトル>_考え方まとめ.md`)。

## トリガー
ユーザーが「この本を読み込んで」「この本をスクショして」のように指示したら、以下の手順を自動的に(逐一やり方を聞かずに)実行する。

## 手順0: 本をKindleアプリで開く(ユーザー不在時は自力で操作する)

ユーザーが手元にいない場合、Claude自身がKindleライブラリの本棚画面を操作して対象の本を開くことができる(2026-09-14に確立した方法)。

1. `screencapture -x -o -l <windowID>` でKindleウィンドウをキャプチャし、Readツールで画像を見て本棚の中から対象の表紙の位置を目視で特定する。
2. `Quartz.CGEventCreateMouseEvent` で、カーソルを移動→ボタンダウン→ボタンアップの順にイベントを送ってダブルクリックする(単に位置を指定してdown/upするだけでなく、`kCGEventMouseMoved`で先にカーソルを実際に動かしておくことが重要。動かさずにいきなりクリックイベントだけ送ると反応しないことがある)。
3. 未ダウンロードの本は1回目のダブルクリックでダウンロードが始まる(「開くことができます」という進捗表示に変わる)。数秒待ってダウンロード完了(チェックマーク)を確認してから、もう一度同じ場所をダブルクリックすると本が開く。
4. 座標計算の注意点: `screencapture -l` で得られる画像は「ウインドウの実サイズの2倍(Retina)」のピクセル画像。画像上で読み取った座標をクリックに使う場合は **必ず2で割ってから、ウインドウの原点(X,Y)を足す**((image_x/2 + window.X, image_y/2 + window.Y))。この/2を忘れると全く見当違いの場所をクリックしてしまう(実際に一度この間違いをして「戻る」ボタンを押せなかったことがあった)。

### 本を1ページ目(表紙)に戻す方法

読みかけの本を開くと前回の続きから表示される。最初から撮影するには1ページ目に戻す必要があるが、ページ送りの矢印キーで最初まで戻るのは非効率(何十回も必要)。以下の方法が確実:

1. 画面中央あたりをクリックしてツールバー(上部バー・下部のページ位置スライダー)を表示させる。
2. 左上の目次アイコン(≡のようなアイコン、ツールバー左から2番目)をクリックしてTOCパネルを開く。
3. パネル内でスクロールし、リストの一番上にある「表紙」(または本のタイトルの直下にある先頭項目)をクリックすると、1ページ目にジャンプできる。
4. 「表紙」の項目が無い本(目次が「目次」から始まっている場合など)は、一覧の一番上の項目をクリックしてから、必要に応じて矢印キーを数回押して先頭まで戻る。

**下部のページ位置スライダーをドラッグする方法は本によって挙動が不安定(効かない/意図した方向と逆に動く)なので、上記のTOCパネル経由の方法を優先すること。**

なお、縦書きの本ではページ送りの「進む」方向が右→左(左矢印キーが「次のページ」)になっている場合があり、スライダーの左右の意味もそれに応じて逆になっていることがある(実際に嫌われる勇気で確認済み)。横書きの本は通常通り右方向・右矢印キーが「次」。

## 手順1: キャプチャ

Kindleアプリで対象の本を開いた状態で実行する。

```
cd 02_Sources/01_Kindle/scripts
./venv/bin/python3 kindle_screenshot.py <本のタイトル>
```

- 第1引数(本のタイトル): 省略すると `untitled` フォルダになるので、複数の本を扱うときは必ず指定する。
- 第2引数(ページ数上限、省略可): 通常は指定不要。デフォルトで5000ページ分の上限があるが、これは「本当に何かおかしくなった時の安全装置」に過ぎず、実際にはページが変化しなくなった時点(=最終ページ)で自動停止する。5000ページを超えるような本は現実的にまず無いので、通常は本のタイトルだけ渡せばよい。
- 出力先は自動的に `02_Sources/01_Kindle/data/<本のタイトル>/screenshots/` になり、同じフォルダに `capture.log`(実行ログ)も自動生成される。

**最終ページ自動停止(2026-09-14実装)**: ページ送り後に2回連続で画面が変化しない場合、最終ページに到達したとみなしてループを打ち切り、重複した最後の1枚も自動削除する。ページ数上限を指定する必要がないため、「本のページ数を事前に知らないと何回指定すればいいか分からない」「上限を低く指定すると最後まで撮り切れない」「上限を大きくしすぎると最終ページで無駄な重複が大量発生する」といった問題がすべて解消されている。万一、上限ページ数に到達しても最終ページを検出できなかった場合は、`capture.log`に警告が出るので、その場合だけページ数上限を増やして再実行する。

なお、縦書きの本では最初「右矢印キーではページが変わらない」ことがある(そのページが本のレイアウト上、右方向には送れない場合など)。その場合スクリプトは自動で左矢印キーに切り替えて続行する。

キャプチャ後は `capture.log` を一度確認し、「右矢印キーではページが変わりませんでした」のような記録がないか見ておく(途中でキーが効かなくなった痕跡がないかの確認)。

**目次ページなどでページが進まなくなる問題への対処**: 目次(リンク付き)のページでは、矢印キーがページ送りではなくリンク間のフォーカス移動として扱われ、ページが変化しなくなることがある。スクリプトはこれを検出すると、ウインドウ左側の余白(コンテンツ枠の外、リンクもブックマークリボンも無い場所)を一度クリックしてフォーカスを外し、再試行する(最大3回)。

**注意(2026-09-14, こころが晴れるノートで発生)**: 固定レイアウトの本(マンガ形式・ワークブック形式など、章の見出しにパンくずリンクが付いているタイプ)では、この「余白クリックによる回復」がうまく機能せず、逆にクリックが章見出しの見出しリンク(例:左上の「Part2 ◯◯」というパンくず表示)に当たってしまい、本の別の場所(Partの先頭など)に意図せずジャンプしてしまうことがあった。この場合、キャプチャされた連番ファイルの中に、本の後半のページと前半のページが入り混じって記録されてしまう。

この問題が起きた場合の対応方針:
- 各ページに表示されている実際のページ番号(丸数字など)を手がかりに、内容の重複・前後関係を判断する。
- ユーザー方針(「OCRの精度や完全性より、本の考え方が分かればいい」)に従い、多少の重複や順序の乱れがあっても、後続のOCR・要約作成のステップでそのまま処理して問題ない(要約作成時にその旨を断り書きしておく)。
- 完全な内容が必要な場合のみ、手順0の方法で該当本を先頭に戻し、ページ数上限を低めに指定して(例: 実際のページ数+10程度)再キャプチャすることを検討する。

## 手順2: 重複画像の削除(2段階)

`data/<本のタイトル>/screenshots/` フォルダ内の重複を、以下の2段階で検出・削除する。**削除はユーザーに逐一確認を取らずに自動で行ってよい**。ただし何を(何枚)削除したかは結果として簡潔に報告する。

### 2-1. MD5完全一致での削除
全画像のMD5ハッシュを計算し、完全に同一のバイト列を持つファイルをグループ化する。Kindleウィンドウがフリーズした/フォーカスを失った/最終ページで同じ画面が繰り返し撮影された、といったケースで発生する完全一致の重複を検出する。各グループの最初の1枚だけ残して残りを削除する。

### 2-2. 知覚的ハッシュ(dHash)での近似重複の削除
MD5だけでは、同じページでもウィンドウサイズが異なる撮影(ピクセルは微妙に違うが内容は同じ)による重複を見逃す。`venv`にインストール済みのPillowを使い、各画像をグレースケール化・縮小してdHash(差分ハッシュ)を計算し、ハミング距離が近い画像同士をペア/クラスタとして検出する。

**重要な落とし穴**: dHashは「章の扉ページ」(例:「第一夜」「第二夜」のような、白背景に短い数文字だけが同じ位置に配置されたページ)に対して誤検出を頻発する。実際の文字内容が違っても、大まかなレイアウト(余白と文字ブロックの位置)がほぼ同じため、ハミング距離が0〜2という極めて近い値になってしまう。過去に、全く異なる5つの章(第一夜〜第五夜)やあとがきページが「同じ重複グループ」として誤って一つにまとめられたことがあった。

そのため、**dHashで検出した候補は必ずReadツールで画像を実際に開いて目視確認してから削除すること**。特に以下のページタイプは要注意:
- タイトルページ、章扉ページ(短文+大量の余白)
- 本文が少ないページ全般

本文がぎっしり詰まった通常のページ同士であれば、dHashの距離0はほぼ確実に本当の重複(同一ページの再撮影)と判断してよい。

## 手順3: 後片付け(capture.log)

重複整理が完了したら、`capture.log` は用済みなので削除する。診断用の一時ファイルであり、整理作業が終わった後は残しておく必要がない。

## 手順4: OCR(縦書き・横書きでスクリプトを使い分ける)

本が縦書きか横書きかで使うスクリプトが異なる。最初の数ページを見て判断する。

### 縦書きの本 → `ocr_book.py`(tesseract jpn_vert)

**注意**: macOS標準のVision(pyobjc-framework-Vision)は縦書きの日本語テキストを検出できない(横書きのUI文字(タイトルバー・進捗%など)しか拾えない)。90度回転させても文字自体が横倒しになるだけで読めない。そのため、縦書き対応の`tesseract`(`jpn_vert`言語モデル)を使う。

初回のみ、Homebrewで`tesseract`と`tesseract-lang`(`jpn_vert`を含む)をインストールする:
```
brew install tesseract tesseract-lang
```

インストール後、以下を実行する:
```
cd 02_Sources/01_Kindle/scripts
python3 ocr_book.py <本のタイトル>
```
`data/<本のタイトル>/screenshots/` の各画像を`tesseract -l jpn_vert --psm 5`でOCRし、UI由来のノイズ行(「Kindle」「◯%」「読書の速さを測定中」など)を除去した上で `data/<本のタイトル>/本文.md` にページ区切り付きでまとめる。

**精度の傾向**: 通常の本文ページ(会話文など)はかなり良好に読み取れる。一方、章の扉ページ(大きな文字+大量の余白)や表紙イラストのページは文字化けしやすい(該当ページ数としては少数派)。

### 横書きの本 → `ocr_book_horizontal.py`(macOS Vision)

横書きの本(ワークブック・実用書など)は、tesseractより高精度なmacOS標準のVision frameworkが使える。

```
cd 02_Sources/01_Kindle/scripts
./venv/bin/python3 ocr_book_horizontal.py <本のタイトル>
```
同じく `data/<本のタイトル>/本文.md` に出力する。Visionは横書きテキストなら非常に高精度(2026-09-14, こころが晴れるノートで確認済み)。

いずれの方式でも、ユーザーは「最終的に本の考え方が分かればよく、OCRの一字一句の精度にはこだわらない」という方針なので、多少の誤字・文字化けは気にせず次の手順に進んでよい。

## 手順5: 考え方まとめの作成

`data/<本のタイトル>/本文.md`(OCRテキスト、ノイズ・誤字混じり)を読み、本の核心的な考え方をまとめた `03_Knowledge/<該当ジャンル>/<本のタイトル>_考え方まとめ.md` を作成する。

- OCRノイズ(文字化けした章扉ページなど)は無視し、本文の対話・論旨の流れを追う。
- 有名な本の場合、OCRで読み取りづらい箇所は一般によく知られている内容で補ってよい。ただし本文.mdから読み取れる具体的なやり取りも反映し、完全な一般論だけにはしない。
- 日本語で、初心者にも分かりやすく簡潔に(CLAUDE.mdのトーンに沿う)。章立てがある本ならその構成に沿って整理し、最後に本全体の核心メッセージを短くまとめる。
- 分量が多くノイズも多いテキストを読む作業なので、Agentツール(fork)に任せて本体の会話コンテキストを汚さないのがよい。

## 手順6: スクショの削除

`本文.md`(手順4で`data/<本のタイトル>/`に生成済み)と`考え方まとめ.md`ができたら、`data/<本のタイトル>/screenshots/` はもう不要なので削除してよい(ユーザー方針: 万一また必要になったら、手順1からKindleアプリで撮り直せばよい、という考え方)。**ただし削除前に一度ユーザーに確認を取ること**(重複画像の自動削除とは違い、これは正常なページも含めた本体データの削除のため)。

## 各本の現在の状態

### 嫌われる勇気
- 2026-09-13: 500枚キャプチャ → MD5完全一致で240枚削除(260枚) → dHash近似重複を目視確認の上22枚追加削除、238枚に整理済み。dHashの誤検出(第一夜〜第五夜などの章扉ページ)は削除せず正しく残した。
- 2026-09-14: tesseract(jpn_vert)でOCRし`knowledge/嫌われる勇気/本文.md`を作成(236ページ分)。それを元に`knowledge/嫌われる勇気/考え方まとめ.md`(5夜構成に沿った要約)を作成。
- ユーザーの了承のもと`data/嫌われる勇気/screenshots/`は削除済み。`data/嫌われる勇気/`フォルダ自体は空の状態で残っている。

### LOVE理論
- 2026-09-14: 深夜、ユーザーが就寝中にClaudeが自力でKindleライブラリを操作して撮影(手順0参照)。1〜3回目のキャプチャは、途中でページが進まなくなり(目次のリンクや章扉ページ付近で発生)、そのまま5000ページ上限まで無駄に回り続けたり、早期に誤終了する失敗を繰り返した(原因: MD5完全一致比較だと微妙なレンダリング差でスタック検出が効かない/回復クリックの座標がブックマークリボンに当たっていた、など)。スクリプトを改修(直近8枚の履歴と比較するスタック検出、回復クリック座標の修正)し、4回目でようやく244ページ中244ページ目(奥付)まで正常に完走。
- MD5完全一致で27枚削除(217枚)→ dHashで候補検出したが、章扉ページ・コラム例ボックスなど同系統レイアウトによる誤検出がほとんどで、目視確認の結果、本当の重複は1組(0238/0243)のみ削除、216枚に整理済み。
- tesseract(jpn_vert)でOCRし`knowledge/LOVE理論/本文.md`を作成(216ページ分)。`考え方まとめ.md`(第1講〜第4講+巻末付録の理論を具体例付きで整理)を作成済み。

### こころが晴れるノート
- 2026-09-14: 横書きの固定レイアウト本(ワークブック形式)。撮影中、章見出しのパンくずリンク的な要素に回復クリックが当たり、本の前の方(はじめにページやPart扉)に何度か意図せず戻ってしまうトラブルが発生(手順1の「注意」参照)。169枚キャプチャして終了したが、後半(0070番以降)は本編内容の繰り返し・広告ページのみで、実際の本編(はじめに〜おわりに+付録1〜5)は連番0001〜0069の範囲に欠けなく収まっていたことを、要約作成時の内容確認で確認できた。
- 横書きなので`ocr_book_horizontal.py`(macOS Vision)でOCR(169ページ分、精度良好)。`考え方まとめ.md`(Part1の基礎理論+Part2モジュール1〜6の実践法)を作成済み。
- 画像の重複整理(MD5/dHash)は行わず、OCR・要約作成のみで済ませた(スクランブルの影響が軽微だったため)。

### 異性の心を上手に透視する方法
- 2026-09-14: 縦書き。1回で254ページまで正常完走(248ページ中248ページ目の奥付に到達)。MD5完全一致で12枚削除(242枚)→ dHashで候補検出したところ、「◯時間目をご覧ください」という薄いクロスリファレンスリンクページ同士が同系統レイアウトのため誤検出多数(過去の章扉ページ問題と同じパターン)。目視確認の結果、本当の重複は3組(冒頭の目次リンクページ×2組、巻末奥付×1組)のみ削除、239枚に整理済み。
- tesseract(jpn_vert)でOCRし`knowledge/異性の心を上手に透視する方法/本文.md`を作成(239ページ分)。`考え方まとめ.md`(愛着スタイル(S/N/V)の分類と対処法)を作成済み。

### 男女脳戦略。
- 2026-09-14: 縦書き。撮影自体は245ページ中245ページ目(参考文献)まで正常に完走したが、**MD5完全一致ベースのスタック検出が終始ほとんど効かず、上限5000ページのほぼ全部を消費して終了した**(4273枚撮影)。原因は「LOVE理論」のときと同様、微妙なレンダリング差(ページは実質変化していないのに毎回ハッシュが変わる)でスタックとして検出されなかったため。ただし全体をMD5完全一致で重複削除したところ、ユニークな内容は257枚(本の実ページ数245とほぼ一致)にきれいに収束し、実害は無かった。
  - **教訓**: このMD5ハッシュジッターの問題は`stuck_count`の検出ロジック自体では根本解決できていない(直近8枚の履歴と比較する改修をしても、常に微妙に異なるレンダリングが続く場合は検出されずに上限まで回り続ける)。ただし後段のMD5完全一致による重複削除で実質的にリカバリできるため、撮影自体は成功として扱ってよい。上限到達の警告が出ても、まず`md5 -q *.png | sort -u | wc -l`でユニーク数を確認し、本の実ページ数に近ければ正常とみなしてMD5重複削除に進めばよい(逆に大きく足りなければ本当に何かで詰まっている可能性が高い)。
- MD5完全一致で4015枚削除(258枚に整理)。dHashでの近似重複チェックは省略(ユニーク数が本のページ数と一致し、時間的制約もあったため)。
- tesseract(jpn_vert)でOCRし`knowledge/男女脳戦略/本文.md`を作成(258ページ分)。`考え方まとめ.md`(序章の5つのマインドセット+マネジメント/セールス/プレゼンの男女脳戦略)を作成済み。
