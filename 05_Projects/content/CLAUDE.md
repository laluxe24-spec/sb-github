# content(恋愛系 X→note発信プロジェクト)

このフォルダで作業する前に必ず`docs/操作メモ.md`を読むこと。

- 実績が無い前提のポジショニング(「モテてる人」ではなく「研究してまとめてる人」)を崩さない。誇張・断定的な自慢文言は使わない。
- はやと式・乾式などKnowledge内の元ネタの言い回しをそのまま投稿に使わない(パクリに見えるリスク)。技術・型だけ抽出して、文言は毎回作り直す。
