# AI組織図

sb-macで動いてるAI(自動化の担当プロセス)を、部署とメンバーの形で整理したもの。りゅーじ君は個別のAIを意識せず、秘書AI(このセッション)を窓口にやり取りする。

## 共通の設計図(どのメンバーも必ずこの形)

新しいメンバーを追加する時は、必ずこの5段階に沿わせる。中身の分析方法(判断ロジック)は自由だが、この骨組みは崩さない。これが崩れるとメンバーごとにバラバラの形式になり、収拾がつかなくなる。

```
1. 新着チェック(前回からの差分だけを見る)
2. 目的にあった分析(メンバーごとに中身は違う)
3. 決まった形式で保存(見た目は読みやすい文章のまま)
4. 内部メタデータ更新(裏側の管理情報)
5. 処理済み記録(次回同じものを見ないように)
```

## 今動いてる部署

### リソース部署(情報を集める)

| メンバー | 役割 | 実行タイミング |
|---|---|---|
| リソースTikTok AI | `@eigyou.inui`の新着動画を文字起こし | 毎日5:00 |
| リソースX AI(投稿) | 投稿型7アカウントの新着ポストを収集 | 毎日5:15 |
| リソースX AI(kw1ew) | kw1ewの新着スクショを収集・一次選別 | 毎日5:30 |
| リソースKindle AI | 本を渡されたら文字起こし | 頼まれた時だけ(izunaと同じ) |

### ナレッジ部署(集めた情報を知識に変換)

| メンバー | 役割 | 実行タイミング |
|---|---|---|
| ナレッジ行動AI | X投稿7アカウント・TikTokを横断比較し、共通する原則を`X_行動テクニック.md`/`04_Sales`に統合 | 毎日5:45 |
| ナレッジ会話AI(kw1ew) | kw1ewのスクショを構造分解し、`X_会話テクニック.md`(人物像+型)を育てる | 毎日5:55 |
| チェックAI | 上2人の仕事を事実確認・重複チェック・軽微な修正 | 毎日6:00 |

## 増やし方の方針

- **行動系のAI**: ソースが増えるだけなら増やさない(今のAIの対象フォルダに追加するだけ)。本格的に新しいジャンル(営業・マーケなど、恋愛以外)が増えたらジャンルごとに1人追加。
- **会話系のAI**: 新しい「人」の会話を分析し始めるたびに1人追加(人物像がブレないように、人ごとに専用)。
- **チェックAI**: 当面1人のまま。1日の変更量が多すぎて見きれなくなったら、分析AIと同じ単位(ジャンル別・人別)で分ける。
- どのメンバーも、上記の「共通の設計図」5段階には必ず沿わせる。

## 将来追加予定(今は着手しない)

| 部署 | 予定メンバー | 役割 |
|---|---|---|
| リソース部署 | リソースInstagram AI | インテリア系投稿の分析(なぜバズるか、画角・構図など) |
| リソース部署 | リソースWeb AI | 恋愛に限らず幅広く新しい情報を収集 |
| ナレッジ部署 | ナレッジ要約AI(Kindle) | 本1冊を通しで理解し、要約を作る(行動AIとは別物、横断比較ではなく単体の要約という別の作業のため) |
| ナレッジ部署 | ナレッジ画像分析AI(Instagram) | 画像そのものを見て、構図・バズ要因を分析(会話AIに近いが対象が写真) |
| プロジェクト部署(発信・制作) | 画像生成AI | Instagram分析を踏まえて実際に画像を作る |
| プロジェクト部署(発信・制作) | note/X発信AI | ナレッジを元に、note販売・X発信用のコンテンツを作る |

「ナレッジまでは今の仕組みで進める、発信・収益化(プロジェクト部署)は別のタイミングで着手する」という優先順位。
