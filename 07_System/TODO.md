# TODO(保留中のタスク)

## 全体方針(2026-09-18・重要)

ナレッジの分け方を「行動系/会話系/要約系」という内容タイプ別から、「情報源(誰の情報か)」別に変更する。

- 乾さん → `乾式.md`
- はやとさん → `はやと式.md`
- 本 → タイトルごとに`.md`
- 複数アカウント横断 → `共通点メモ.md`(Sourceのまま、Knowledge化しない)

この結果、今まで使ってた共有の`判断.md`・`行動.md`・`会話.md`は実質不要になる見込み。既存の中身(書籍由来の記述など)は、それぞれ新しい専用ファイルに移す必要がある。`knowledge_agent_prompt.md`のルール全体を、情報源ベースの分岐に書き換える必要あり(単なる保存先の変更ではなく、判定ロジックの土台からの変更)。

**目的の分離について(将来のため)**: ナレッジ化の判断基準は目的によって2つに分かれる。

- **成長用(今運用してるのはこっち)**: りゅーじ君自身のためのKnowledge。乾式・はやと式・本のタイトル別など、今日設計した仕組み全部がこれ
- **副業成功用(将来)**: 例えば「Xでバズってるリソースを集める」場合、それは自分の成長のためではなく、副業(発信)を成功させるための解析(なぜバズるか、何が売れるか)。判断基準も成長用とは別にする

データが増えてから混ざると分けるのが大変になるので、この「将来2つの目的で判断基準を分ける」という方向性を、今のうちから前提として意識しておく(今すぐ実装はしない)。

## 1. 乾・はやとの専用ファイル化(2026-09-18)

**内容**: 乾さん(TikTok @eigyou.inui)とはやとさん(X @kw1ew)は、りゅーじ君が特に信頼してる発信者。新しいトップの情報源フォルダは作らず、既存のジャンルフォルダにそのまま入れる。

- 乾さん → `03_Knowledge/04_Sales/乾式.md`(新規の専用ファイル)
- はやとさん → `03_Knowledge/03_Romance/はやと式.md`(新規の専用ファイル)

今の「行動.md内に`## 乾式`のような見出しを立てるだけ」の形式ではなく、それぞれ専用の.mdファイルを1つずつ作り、その中に分析済みデータを入れる形にする(フォルダではなくファイル単位)。

**収集(生データ)は変更なし**:
- 乾さん → 通常のTikTok日次収集(`02_Sources/03_TikTok/data/@eigyou.inui`)のまま
- はやとさん → 専用のkw1ew日次収集(朝5:30、`02_Sources/02_X/data/@kw1ew`)のまま

**変わるのはナレッジ化(朝5:40の処理)の保存先だけ**。

**影響範囲(実行時に要確認)**:
- `knowledge_agent_prompt.md`の保存形式ルール(1-A章)を書き換え、乾さん・はやとさんの場合だけ`乾式.md`/`はやと式.md`に保存するよう指示を追加
- 既存の`03_Knowledge/04_Sales/行動.md`・`03_Knowledge/03_Romance/行動.md`内に、既に乾式・はやと式の見出しで保存済みの内容があれば、新しい専用ファイルに移す

**継続運用**: 今回の移行(既存データの引っ越し)だけでなく、今後も乾さん・はやとさんの新着は継続してそれぞれの専用ファイル(乾式.md/はやと式.md)に貯め続ける仕様にする。knowledge_agent_prompt.mdのルーティングを恒久的に変更する。

**並び順**: 新しい情報ほどファイルの上に来るようにする(下に積み足す今までの形式と逆)。

**ステータス**: 未着手(方針確定、実行待ち)

---

## 2. 本のタイトル別.mdへの復帰(2026-09-18)

**内容**: 現在、本由来のKnowledgeはジャンル別の判断.md/行動.mdに統合される形になってる(2026-09-17〜18に統合済み)。これを再び本のタイトルごとの独立した.mdファイルに戻す。

**理由**: りゅーじ君いわく「(本ごとに分かれてた方が)見やすい」。

**注意**: これは直近で明示的に決めた統合ルールと逆方向の変更。knowledge_agent_prompt.mdの3章(要約系の処理)の「単体の全体要約ファイルも作らない」ルールを書き換える必要あり。

**確定事項**: 新しいフォルダは作らない。本ごとの.mdファイルを、今まで通り一番合うジャンルフォルダの中にそのまま置く(例: `03_Knowledge/02_Psychology/嫌われる勇気.md`、`03_Knowledge/03_Romance/LOVE理論.md`)。ジャンルをまたぐ内容(1冊の本から判断・行動・会話が両方出る場合)をどう扱うかは実行時に要検討。

**ステータス**: 未着手(方針確定、実行待ち)

---

## 3. 今日の新着まとめをsb-mac内に出す(2026-09-18)

**内容**: 毎朝のナレッジ化(knowledge-agent-daily)が「今日何を追加したか」のまとめを作ってるが、今は`sb-mac-logs/knowledge-agent.log`という別フォルダのログにしか出てない(Obsidianで見えるsb-mac本体には出てこない)。これをsb-mac内の見える場所に短くまとめて書き出すようにする。

**ステータス**: 未着手(方針確定、置き場所は未定)

---

## 4. 共通点メモ.mdも新しい情報を上に積む(2026-09-18)

**内容**: 複数アカウント横断の経験則をまとめてる`共通点メモ.md`(例: `02_Sources/02_X/共通点メモ.md`)も、乾式.md・はやと式.mdと同じく新しい情報ほど上に来る並び順にする。

**ステータス**: 未着手(方針確定、実行待ち)

---

## 5. 裏付けデータ取得(Research)の自動化(2026-09-18)

**目的**: メンタリストDaiGoさんのように、根拠(学術研究など)を示せる状態を目指す。今は「気になった時に都度お願いする」単発タスクだが、これを自動化する。2つのパターンがある。

### パターン1: 受動的(乾・はやと・気になった本など、りゅーじ君のインスピレーションで良いと思ったもの)

新しくKnowledgeに追加された情報について、追加の都度、自動で学術的な裏付けを調べに行く。見つけたデータは`02_Sources/07_Documents/成長用裏付け.md`にまとめて保存する(新しい順)。「成長用」と名付けるのは、将来「副業用」の裏付けファイルと分ける前提のため。

### パターン2: 能動的(倫理学・社会心理など、ちゃんと勉強したい分野)

りゅーじ君が「社会心理について調べて」のようにジャンルを指定したら、その分野の論文・資料を能動的に集める。

- **収集先(Resource)**: `02_Sources/07_Documents/<ジャンル名>/`(例: `02_Sources/07_Documents/社会心理学/`)に、興味のあるジャンルごとにフォルダを作り、集めた生の資料を入れる
- **蓄積先(Knowledge)**: `03_Knowledge/<該当ジャンル>/<ジャンル名>.md`(例: `03_Knowledge/02_Psychology/社会心理学.md`)に、要約・分析したデータを新しい順(上に追加)で貯めていく

**イメージ**: knowledge-agent-daily(5:40)→knowledge-review-daily(6:00)と同じように、その後に自動で動くジョブを追加する形(パターン1)。パターン2は都度お願いする形(将来的に自動化も検討)。

**ステータス**: 未着手(方針確定、詳細設計は実行時)
