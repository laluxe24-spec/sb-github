# 用語集

作業中に出てきた「これ何?」をジャンルごとに貯めておくメモ。
新しい用語が出てきたら、当てはまるジャンルが無ければその時にジャンルを追加していく。
※このファイルはClaudeが自動で読み込むものではなく、必要な時に自分で見るための辞書。

## 何かを動かす(プログラムを実行する)時に使う環境

### Python(パイソン)
プログラムを書くための言語の1つだよ。

- 人間が読み書きできる言葉でプログラムを書く
- それをコンピューターが実行することで、自動で作業をしてくれる
- Kindleのスクショを撮るスクリプト(`kindle_screenshot.py`)は、このPythonで作られている

### venv(ブイイーエヌブイ)
Pythonを動かすための、専用の作業スペースのようなものだよ。

- プロジェクトごとに、必要な道具(ライブラリ)を分けて用意できる
- 別プロジェクトの道具と混ざらないので、壊れにくい

### JavaScript / Node.js
これもPythonと同じ、プログラムを書くための言語の1つ。

- X解析のスクリプト(`collect_media.js`など)はこちらで作られている
- 同じ「プログラムを動かす」でも、Kindleと X解析では使ってる言語が違う、というだけ

## 何かを保存・記録する時に使うもの

### Git(ギット)
ファイルの変更履歴を記録・管理する仕組みだよ。イメージとしては、作業のセーブポイントを作れる感じ。

- ファイルを変更する
- 「ここまでの変更を保存」って記録する(これを**コミット**と呼ぶ)
- 後から「いつ・何が変わったか」を確認したり、必要なら前の状態に戻したりできる

### GitHub(ギットハブ)
Gitで管理してるプロジェクトを、インターネット上に保存・共有できるサービスだよ。

- 手元のパソコンだけじゃなく、外のサーバーにもバックアップされる
- 他の人やAI(Claude)と同じファイルを共有できる

### リポジトリ
1つのプロジェクトの、Gitで管理されているファイル一式のことだよ。`sb-mac`フォルダ全体が1つのリポジトリ、というイメージ。

### コミット
「ここまでの変更を記録として保存する」操作だよ。変更の節目ごとに行う、セーブポイント作成のこと。

### プッシュ
手元(ローカル)で作ったコミットを、GitHub(リモート)側へ送って反映させることだよ。

- コミット = 手元でセーブ
- プッシュ = そのセーブをインターネット上にも送る

## ファイルの場所を表す言葉

### パス(path)
ファイルやフォルダの「住所」のことだよ。

- そのファイルがコンピューターのどこにあるかを、フォルダをたどる道順で表した文字列
- 例: `X解析/data/screenshot_accounts/kw1ew/スクショ/001.jpg`(X解析フォルダ→dataフォルダ→…とたどっていく道順そのもの)
- フォルダを移動すると、そのファイルの「住所」も変わるので、操作メモなどに書いてあるパスの記述も合わせて書き直す必要がある

## ファイルの種類

### .md(マークダウン)
見出しや箇条書きが書ける、シンプルなテキストファイル形式だよ。

- `# 見出し`と書くと見出しになる
- `- 箇条書き`と書くと箇条書きになる
- このプロジェクトの操作メモやREADMEは全部この形式で書かれている

### .py
Pythonのプログラムが書かれたファイルだよ。

### .js
JavaScriptのプログラムが書かれたファイルだよ。

## sb-macのフォルダのルール

### docs
「documents(説明書類)」の略だよ。プロジェクトの説明を置く場所、という意味。

- `README.md`(概要)と`操作メモ.md`(詳しい手順)の2つを置く
- どのプロジェクトにも必ず作る、必須のフォルダ

### README.md
プロジェクトの概要(何をするものか)を短くまとめたファイルだよ。詳しいやり方は書かず、あくまで入り口の説明だけ。

### 操作メモ.md
Claudeが作業を再現するための、詳しい手順書だよ。

- どんな指示が来たら動くか(トリガー)
- 実際の手順
- 過去にハマった注意点

がまとまってる。

### scripts
「script(台本・脚本)」の複数形だよ。自動で実行するプログラムのファイルを置く場所。

- 必要な時だけ作る
- 中身(`.py`や`.js`)を消すと、その自動化ができなくなるので注意
- 操作メモ.mdが「人が読む手順書」なのに対して、scriptsは「機械がそのまま実行するプログラム本体」という違いがある

### data
収集した生データ(画像・取得ファイルなど)そのものを入れる置き場だよ。

- 「やったこと」の記録ではなく、実際に集めた実物のファイルが入る
- 必要な時だけ作る
- knowledgeに分析結果をまとめた後も、後で検証・再分析できるように消さずに残しておく

### knowledge
生データ(data)を分析してまとめた、結論・レポートの置き場だよ。

- 必要な時だけ作る
- 生データと生成物を混ぜないために、あえて分けている

### docs / scripts / data / knowledge(4つの役割まとめ)
このプロジェクトのフォルダは、基本この4種類で揃える。

- `docs`(説明書類)だけは全プロジェクト必須
- 残り3つ(`scripts`/`data`/`knowledge`)は、そのプロジェクトの内容に応じて必要な時だけ作ればいい
